background {
    monitor =
    path = /home/exordinant/Wallpapers/CapeCanaveral.png
    blur_passes = 2

}


# TIME
label {
    monitor =
     text = cmd[update:1000] echo "$(date +"%-I:%M%p")"
    color = rgba(185, 187, 38, .75)
    font_size = 120
    font_family = SF Pro Display Bold
    position = 0, -140
    halign = center
    valign = top
}

# DAY-DATE-MONTH
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date '+%A, %d %B')</span>"
    color = rgba(185, 187, 38, 0.75)
    font_size = 30
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, 200
    halign = center
    valign = center
}

# LOGO
#label {
    #monitor =
    #text =  
    #color = rgba(255, 255, 255, 0.65)
    #font_size = 120
    #position = 0, 60
    #halign = center
    #valign = center
}

# USER
label {
    monitor =
    text = This Computer is Locked
    color = rgba(255, 255, 255, .65)
    font_size = 25
    font_family = JetBrainsMono Nerd Font Mono
    position = 0, -70
    halign = center
    valign = center
}

# INPUT FIELD
input-field {
    monitor =
    size = 290, 60
    outline_thickness = 2
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgba(0, 0, 0, 0)
    inner_color = rgba(58, 60, 54, 0.35)
    font_color = rgb(200, 200, 200)
    fade_on_empty = false
    font_family = JetBrainsMono Nerd Font Mono
    placeholder_text = <i><span foreground="##ffffff99">Enter Password</span></i>
    hide_input = false
    position = 0, -140
    halign = center
    valign = center
    check_color = rgb(254, 128, 24)
    fail_color = rgb(251, 73, 52)
